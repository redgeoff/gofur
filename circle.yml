# Doesn't actually work!
# node:
#  version: 8.7.0

dependencies:
  pre:
    - nvm install 8.7.0
    - nvm use 8.7.0 && npm install -g npm

  override:
    - nvm use 8.7.0 && npm install

test:
  pre:
    - nvm use 8.7.0 && npm run assert-beautified
    - nvm use 8.7.0 && npm run jshint

  override:
    - nvm use 8.7.0 && npm run node-full-test
    - nvm use 8.7.0 && npm run browser-test-phantomjs
    - nvm use 8.7.0 && npm run browser-coverage-full-test
