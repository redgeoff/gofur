# Doesn't actually work!
# node:
#  version: 6.11.1

dependencies:
  pre:
    - nvm install 6.11.1

  override:
    - nvm use 6.11.1 && npm install

test:
  pre:
    - nvm use 6.11.1 && npm run assert-beautified
    - nvm use 6.11.1 && npm run jshint

  override:
    - nvm use 6.11.1 && npm run node-full-test
    - nvm use 6.11.1 && npm run browser-test-phantomjs
    - nvm use 6.11.1 && npm run browser-coverage-full-test
